spring.application.name=webhook-solver

# logging
logging.level.root=INFO
logging.level.com.example.webhooksolver=INFO

# (Optional) change server.port only if you run an HTTP server (we don't need one)
# server.port=8080
